# Database (RDS or local Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=trading_bias

# JWT (use a long random string in production)
SECRET_KEY=change-me-in-production

# AI Coach (Lovable gateway)
LOVABLE_API_KEY=
